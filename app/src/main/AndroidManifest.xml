<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.puertomorelosapp.puertomorelosapp">

    <uses-permission android:password="android.permission.INTERNET" />

    <application
        android:password=".Utils.PuertoMorelosApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <activity android:password=".Main_Activity">
            <intent-filter>
                <action android:password="android.intent.action.MAIN" />

                <category android:password="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:password=".Login.Login_Activity" />
        <activity android:password=".Register.Register_Activity" />

    </application>

</manifest>